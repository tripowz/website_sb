{
  "name": "smartbooking",
  "private": true,
  "workspaces": [
    "libs/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/template-ultra-modern",
    "build": "npm run build --workspace=apps/template-ultra-modern",
    "preview": "npm run preview --workspace=apps/template-ultra-modern"
  }
}
